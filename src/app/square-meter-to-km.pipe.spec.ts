import { SquareMeterToKmPipe } from './square-meter-to-km.pipe';

describe('SquareMeterToKmPipe', () => {
  it('create an instance', () => {
    const pipe = new SquareMeterToKmPipe();
    expect(pipe).toBeTruthy();
  });
});
