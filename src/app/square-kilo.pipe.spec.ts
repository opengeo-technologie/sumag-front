import { SquareKiloPipe } from './square-kilo.pipe';

describe('SquareKiloPipe', () => {
  it('create an instance', () => {
    const pipe = new SquareKiloPipe();
    expect(pipe).toBeTruthy();
  });
});
